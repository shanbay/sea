{%- if not skip_orator -%}
git+https://github.com/shanbay/orator.git@develop#egg=orator
mysqlclient
{% endif -%}
{%- if not skip_cache -%}
redis
{% endif -%}
{%- if not skip_consul -%}
python-consul
{% endif -%}
{%- if not skip_celery -%}
celery
{% endif -%}
pytest
